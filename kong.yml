_format_version: "3.0"

services:
  - name: api-service
    url: http://api-service:8080
    routes:
      - name: api-route
        paths:
          - /api

  - name: auth-service
    url: http://auth-service:8081
    routes:
      - name: auth-route
        paths:
          - /auth
